[project]
name = 'algorithmic-composition-assistant'
description = ''
authors = []
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.generative-music-model]
path = 'contracts/generative-music-model.clar'
clarity_version = 3
epoch = 3.1

[contracts.orchestration-recommendation-engine]
path = 'contracts/orchestration-recommendation-engine.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
